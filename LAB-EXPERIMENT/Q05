def affine_decrypt(ciphertext):
    a_inv = 9   # Inverse of 3 mod 26
    b = 15

    plaintext = ""

    for ch in ciphertext:
        if ch.isalpha():
            c = ord(ch.upper()) - ord('A')

            # Decryption formula: P = 9(C - 15) mod 26
            p = (a_inv * (c - b)) % 26

            plaintext += chr(p + ord('A'))
        else:
            plaintext += ch

    return plaintext


ciphertext = input("Enter the ciphertext: ")

plaintext = affine_decrypt(ciphertext)

print("Decrypted Text:", plaintext)

OUTPUT:
Enter the ciphertext: hello
Decrypted Text: GFQQR
